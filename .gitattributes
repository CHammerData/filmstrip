# Shell scripts must stay LF or they break (CRLF corrupts the shebang / `$'\r'` errors) when
# run in Git Bash on Windows, in Docker, and in CI. Force LF on checkout regardless of platform.
*.sh text eol=lf
