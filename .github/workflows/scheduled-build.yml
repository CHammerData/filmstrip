name: Scheduled Build and Test

# DISABLED: cron removed so it doesn't run (and fail) nightly. Manual-only for now.
on:
  workflow_dispatch: # Allow manual triggering

jobs:
  build-and-test:
    permissions:
      contents: read
      pull-requests: write
    uses: ./.github/workflows/build-and-test.yml
    secrets: inherit
